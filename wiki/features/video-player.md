# 播放器

> 最后更新：2026-07-22
> 覆盖端：Web / Android / iOS / Backend

## 功能概述

视频播放器是 ShortDrama 的核心功能模块，负责短剧内容的播放、控制与交互。用户通过播放器观看短剧视频，执行播放、暂停、停止等操作。

- 核心价值：为用户提供流畅的竖屏短剧播放体验
- 覆盖范围：Web、Android、iOS、Backend

## 入口与路由

| 端 | 入口 | 路由/deeplink | 源文件 |
|----|------|-------------|--------|
| Web | 首页推荐流 / 剧集详情页 | `/play/:videoId` | `web/` [待确认] |
| Android | 首页推荐流 / 剧集详情页 | `PlayerActivity` | `android/` [待确认] |
| iOS | 首页推荐流 / 剧集详情页 | `PlayerViewController` | `ios/` [待确认] |
| Backend | N/A | API endpoint | `backend/src/app/api/player/` [待确认] |

## 核心逻辑

### 流程：播放视频

1. 用户点击视频卡片，跳转播放器页面
   - 源文件：[待确认]
2. 前端向 Backend 发起播放请求 `POST /api/player/start`
   - 源文件：[待确认]
3. Backend 校验请求参数，返回视频流信息
   - 源文件：[待确认]
4. 前端根据返回的视频地址初始化播放器并开始播放
   - 源文件：[待确认]
5. 用户退出播放或切换视频时，前端发送 `POST /api/player/stop` 上报播放结束
   - 源文件：[待确认]

### 边界与异常处理

| 场景 | 处理方式 | 源文件 |
|------|---------|--------|
| 视频加载失败 | 显示错误提示，提供重试入口 | [待确认] |
| 网络中断 | 暂停播放，待网络恢复后提示继续 | [待确认] |
| 视频资源不存在 | 返回错误信息，前端展示友好提示 | [待确认] |

## 多端实现

### Web

- 核心组件：[待确认]
- 关键 hooks：[待确认]
- 与 Backend 交互方式：REST API
- 特殊处理：[待确认]

### Android

- 核心类：[待确认]
- 与 Backend 交互方式：[待确认]
- 特殊处理：[待确认]

### iOS

- 核心类：[待确认]
- 与 Backend 交互方式：[待确认]
- 特殊处理：[待确认]

### Backend

- 核心模块：`backend/src/app/api/player/` 路由处理
- 数据模型：[待确认]
- 外部依赖：[待确认]

## API 接口

该功能依赖以下后端接口：

| 方法 | 路径 | 说明 | 请求体关键字段 | 响应体关键字段 | 源文件 |
|------|------|------|-------------|-------------|--------|
| POST | `/api/player/start` | 开始播放视频（旧路径 `/api/video/play`） | `{ videoId }` | `{ streamUrl, duration }` | `backend/src/app/api/player/` [待确认] |
| POST | `/api/player/stop` | 停止播放并上报播放进度 | `{ videoId, progress, duration }` | `{ success }` | `backend/src/app/api/player/` [待确认] |

### 路径变更记录

| 旧路径 | 新路径 | 变更时间 |
|--------|--------|---------|
| `/api/video/play` | `/api/player/start` | 2026-07-22 |

## 状态管理

| 状态 | 存储方式 | 作用域 | 说明 | 源文件 |
|------|---------|--------|------|--------|
| 播放进度 | [待确认] | 全局 | 记录当前观看位置，支持断点续播 | [待确认] |
| 播放状态 | [待确认] | 页面级 | 播放/暂停/停止/加载中 | [待确认] |

## 依赖关系

### 内部依赖

| 功能 | 依赖方式 | 说明 |
|------|---------|------|
| 首页 Feed | 路由跳转 | 从推荐流进入播放器 |
| 鉴权 | API 调用 | 播放接口需要用户身份校验 |
| 评论 | [待确认] | 播放器可能与评论面板联动 |

### 外部依赖

| 服务 | 用途 | 接入方式 |
|------|------|---------|
| [待确认] | 视频 CDN/存储 | [待确认] |

## 已知限制

| 问题 | 影响 | 记录时间 | 备注 |
|------|------|---------|------|
| 播放器各端代码尚未初始化 | 当前仅完成 API 接口设计 | 2026-07-22 | 各端播放器组件待后续开发 |

---

*本文档由 llm-wiki skill 自动维护，从代码中提取。如有不一致，以代码为准。*
