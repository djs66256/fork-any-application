## 项目定位

当前项目是一个用于 fork 现有应用并推进后续落地的 harness 工程。
覆盖从产品调研、方案沉淀，到多端应用开发与后续迭代的完整流程。

仓库按端划分工作目录，便于将产品、研发和文档协作统一在同一仓库中进行。

## 当前状态

当前仓库以项目骨架和协作约束为主，业务代码与各端工程可能尚未完全初始化。
在未看到真实工程配置前，不要假设构建命令、依赖管理方式或运行入口。

## 目录结构

- `android/`：Android 应用代码与 Android 端专属说明
- `ios/`：iOS 应用代码与 iOS 端专属说明
- `web/`：Web 前端代码与前端专属说明
- `backend/`：后端服务代码与服务端专属说明
- `wiki/`：由 AI 维护的项目知识、背景信息与阶段性结论
- `docs/`：项目文档目录，包括需求、项目跟踪、设计记录、竞品分析等
- `docs/product_research/`：产品调研与竞品分析文档

## 协作约定

- 根目录 `CLAUDE.md` 负责描述全局规则、目录职责和跨端约束。
- 各端如有独立工程规范，应写入对应子目录下的 `CLAUDE.md`。
- 修改代码时，优先遵守就近目录中的说明；子目录规则优先于根目录通用规则。
- 如某端目录仍为空或尚未初始化，不要臆造该端的工程结构、命令或技术栈。

## 开发约束

- 每端代码只能修改对应路径下的文件。
- API 使用 RESTful 设计。
- 禁止硬编码常量，例如 `localhost:3000`、固定 token、固定环境地址等。
- 尽量使用著名开源库来复用已有实现，但新增开源依赖前必须征得用户同意。

## 文档约定

- 产品、需求、竞品、项目推进类内容优先放在 `docs/`。
- 项目运行背景、关键决策、阶段性知识沉淀优先维护在 `wiki/`。
- 新增说明时，尽量写清放在哪个目录、由哪一端负责、是否属于全局规则。

## SKILL 开发

新设计的 skill 放置到 .claude/skills 中。

### 应该

- skill 定义使用 skill 的相对路径，产物使用项目的相对路径
- 内容被划分到子文档时，应该避免在主文档中重复说明其内容
- 使用如下表达一个 subagent 定义：
```
Subagent：
  description: "采集：<频道>-<功能模块>-<描述>"
  prompt: |
    <your prompt>
```

### 反模式

- 使用绝对路径
- skill 定义文件使用项目的相对路径
- 在 references/ 中说明的内容在 SKILL.md 中又说了一遍，template 在使用的地方又声明了一遍

## Git

git commit 只提交当前会话改动的文件。

## 产品信息

产品信息统一维护在 `PRODUCT.md` 中。
skill、subagent、CLAUDE.md 等元内容文件不得内嵌任何具体的产品信息(比如 ShortDrama，红果 等)，应改为引用 `PRODUCT.md`。
