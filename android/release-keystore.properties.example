# Release 签名配置示例
# 上线前请：
# 1. 将实际 keystore 文件放入 keystore/ 目录
# 2. 重命名本文件为 release-keystore.properties
# 3. 填入正确的密码和别名

storeFile=../keystore/release.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
