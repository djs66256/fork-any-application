# PRD 审查报告：搜索发现

> 审查日期：2026-07-25
> 审查人：AI Agent（产品需求审查专家）

---

## 审查摘要

| 项目 | 结果 |
|------|------|
| PRD 文档 | `docs/product_manager/prd/2026-07-25-search/prd.md` |
| 子任务文档 | `docs/product_manager/prd/2026-07-25-search/subtasks.md` |
| 审查结论 | ✅ 通过（第 1 轮 8 个问题已全部修正） |
| 阻塞问题（🔴） | 3 |
| 建议问题（🟡） | 5 |
| 待澄清问题（Q-XX） | 3 |

---

## 逐维度详细审查

### 1. 完整性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 需求背景是否清晰 | ✅ | 现状、痛点、竞品参考均有描述，目标用户 2 角色覆盖完整 |
| 用户故事是否完整 | ⚠️ | 4 个用户故事均有验收标准和平台标注。但 US-04 快捷入口数量表述不一致（声称 5 个实列 4 个），见 R-01 |
| 范围定义是否明确 | ✅ | 范围内/范围外表格清晰，每项有原因说明 |
| 核心流程是否清晰 | ⚠️ | US-01/02 流程完整（含 mermaid 图），但 US-03/04 缺少用户旅程，见 R-02 |

### 2. 可落地性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 子任务拆分是否合理 | ⚠️ | 6 个 subtask 均有工作内容+完成标准。但 ST-05/ST-06 依赖过重，见 R-04 |
| 工时估算是否超标 | ✅ | 所有子任务均在 5 人日以内（最大 2.5 人日） |
| 依赖关系是否明确 | ⚠️ | 内部依赖图清晰。但跨 PRD 依赖有缺失（R-03）和不合理之处（R-04）；快捷入口导航目标缺失（R-05） |
| 技术可行性检查 | ⚠️ | 搜索 API 可行（wiki 已有 Dramas API 骨架）。但搜索匹配字段与现有 Schema 不一致（R-06）；热搜 API 的数据来源逻辑需明确（R-07） |

### 3. 跨端一致性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 涉及平台是否完整 | ✅ | Backend/iOS/Android 均有覆盖，Web 端不涉及（符合 Sprint 2 规划） |
| 各端交互是否对齐 | ⚠️ | PRD 描述搜索结果卡片「同 Feed 卡片」但 subtasks 未明确复用策略，见 R-08 |

### 4. 冲突检测

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 与现有功能的冲突 | ✅ | `GET /api/dramas/search` 和 `GET /api/dramas/hot-search` 为新增端点，不与现有 API 冲突 |
| 与进行中功能的冲突 | ✅ | progress.md 中无 building 状态条目，无进行中冲突 |

---

## 发现的问题及修正

### R-01：快捷入口数量不一致

| 属性 | 值 |
|------|-----|
| **维度** | 完整性 |
| **严重程度** | 🔴 阻塞 |
| **状态** | ✅ 已修正 | **修正内容：** 
- R-01: PRD 中所有「5 个」改为「4 个」，删除省略号，对齐实际快捷入口数量
- R-02: 新增 4.2（US-03 热搜榜浏览）和 4.3（US-04 快捷入口导航）核心流程章节，包含用户旅程和关键边界表
- R-03: ST-04 前置依赖补充 `PRD-01 ST-05`（Android NavHost 路由框架）
- R-04: ST-05/ST-06 前置依赖改为路由框架（PRD-01 ST-01/ST-02/ST-04/ST-05），不再依赖完整首页 Feed UI
- R-05: 「演员」和「新剧」入口已定义跳转行为：点击展示 Toast「功能开发中」
- R-06: Q-01 结论改为「首版搜标题+分类（category）」对齐现有 Schema
- R-07: ST-02 热搜 API 数据来源明确为首版硬编码种子数据，不与搜索 API 绑定
- R-08: ST-03/ST-04 搜索结果页明确复用首页 Feed 卡片组件
| **问题描述** | PRD 多处声称「5 个快捷入口」但实际仅列出 4 个。第 1.1 节竞品描述为「5 个快捷入口（场景识别/排行/新剧/演员/分类）」，第 1.4 节将「场景识别」列为范围外，第 3 节 US-04 写「5 个快捷入口（排行/新剧/分类/演员）」——括号内仅 4 个，第 4.1 节 UI 表格写「5 个横向图标+文字入口（排行/新剧/分类/演员…）」含省略号。数量自相矛盾。 |
| **建议修正** | 两种方案：(a) 若场景识别不在范围内，将所有「5 个」改为「4 个」，删除省略号；(b) 若确实需要 5 个，补充第 5 个入口的定义和目标页面。推荐方案 (a)，因为场景识别已明确为范围外。 |

### R-02：核心流程覆盖不完整

| 属性 | 值 |
|------|-----|
| **维度** | 完整性 |
| **严重程度** | 🟡 建议 |
| **状态** | ✅ 已修正 | **修正内容：** 
- R-01: PRD 中所有「5 个」改为「4 个」，删除省略号，对齐实际快捷入口数量
- R-02: 新增 4.2（US-03 热搜榜浏览）和 4.3（US-04 快捷入口导航）核心流程章节，包含用户旅程和关键边界表
- R-03: ST-04 前置依赖补充 `PRD-01 ST-05`（Android NavHost 路由框架）
- R-04: ST-05/ST-06 前置依赖改为路由框架（PRD-01 ST-01/ST-02/ST-04/ST-05），不再依赖完整首页 Feed UI
- R-05: 「演员」和「新剧」入口已定义跳转行为：点击展示 Toast「功能开发中」
- R-06: Q-01 结论改为「首版搜标题+分类（category）」对齐现有 Schema
- R-07: ST-02 热搜 API 数据来源明确为首版硬编码种子数据，不与搜索 API 绑定
- R-08: ST-03/ST-04 搜索结果页明确复用首页 Feed 卡片组件
| **问题描述** | 第 4 节仅覆盖了 US-01/02（关键词搜索）的用户旅程和 mermaid 流程图。US-03（浏览热搜榜、点击热搜词搜索）和 US-04（通过快捷入口进入子功能）没有对应的用户旅程描述。 |
| **建议修正** | 在第 4 节新增 4.2（US-03：热搜榜浏览）和 4.3（US-04：快捷入口导航）两个子章节，各包含 2-3 步用户旅程描述即可（无需 mermaid 图）。例如 4.2：「1. 用户进入搜索页，在搜索框下方看到热搜榜 Top 10；2. 点击热搜词 → 跳转搜索结果页展示该词搜索结果」。 |

### R-03：ST-04 缺少 Android 路由框架依赖

| 属性 | 值 |
|------|-----|
| **维度** | 可落地性 |
| **严重程度** | 🟡 建议 |
| **状态** | ✅ 已修正 | **修正内容：** 
- R-01: PRD 中所有「5 个」改为「4 个」，删除省略号，对齐实际快捷入口数量
- R-02: 新增 4.2（US-03 热搜榜浏览）和 4.3（US-04 快捷入口导航）核心流程章节，包含用户旅程和关键边界表
- R-03: ST-04 前置依赖补充 `PRD-01 ST-05`（Android NavHost 路由框架）
- R-04: ST-05/ST-06 前置依赖改为路由框架（PRD-01 ST-01/ST-02/ST-04/ST-05），不再依赖完整首页 Feed UI
- R-05: 「演员」和「新剧」入口已定义跳转行为：点击展示 Toast「功能开发中」
- R-06: Q-01 结论改为「首版搜标题+分类（category）」对齐现有 Schema
- R-07: ST-02 热搜 API 数据来源明确为首版硬编码种子数据，不与搜索 API 绑定
- R-08: ST-03/ST-04 搜索结果页明确复用首页 Feed 卡片组件
| **问题描述** | ST-03（iOS 搜索页）在「前置依赖」中正确列出了 `PRD-01 ST-02（路由）`（iOS NavigationStack 路由框架），但 ST-04（Android 搜索页）仅列出 `ST-01, ST-02`，缺少对应的 Android 路由框架依赖。搜索页需要通过 NavHost 注册路由才能被导航到。 |
| **建议修正** | 在 ST-04 的「前置依赖」中补充 `PRD-01 ST-05（Android NavHost 路由框架）`，与 iOS 端对齐。 |

### R-04：ST-05/ST-06 依赖过重

| 属性 | 值 |
|------|-----|
| **维度** | 可落地性 |
| **严重程度** | 🔴 阻塞 |
| **状态** | ✅ 已修正 | **修正内容：** 
- R-01: PRD 中所有「5 个」改为「4 个」，删除省略号，对齐实际快捷入口数量
- R-02: 新增 4.2（US-03 热搜榜浏览）和 4.3（US-04 快捷入口导航）核心流程章节，包含用户旅程和关键边界表
- R-03: ST-04 前置依赖补充 `PRD-01 ST-05`（Android NavHost 路由框架）
- R-04: ST-05/ST-06 前置依赖改为路由框架（PRD-01 ST-01/ST-02/ST-04/ST-05），不再依赖完整首页 Feed UI
- R-05: 「演员」和「新剧」入口已定义跳转行为：点击展示 Toast「功能开发中」
- R-06: Q-01 结论改为「首版搜标题+分类（category）」对齐现有 Schema
- R-07: ST-02 热搜 API 数据来源明确为首版硬编码种子数据，不与搜索 API 绑定
- R-08: ST-03/ST-04 搜索结果页明确复用首页 Feed 卡片组件
| **问题描述** | ST-05 前置依赖为 `PRD-02 ST-03（首页UI）`（3 人日），ST-06 前置依赖为 `PRD-02 ST-04`（3 人日）。搜索入口集成仅需在首页顶部栏添加一个搜索图标+点击导航事件，不需要完整的 Feed 卡片列表、分页加载、下拉刷新等功能完成。这会造成 ST-05/ST-06 必须等首页 Feed UI 全部完成才能开始，不必要地阻塞了并行开发。 |
| **建议修正** | 将依赖改为路由框架+首页占位页面：ST-05 依赖 `PRD-01 ST-01/ST-02`（iOS TabView + NavigationStack），ST-06 依赖 `PRD-01 ST-04/ST-05`（Android NavigationBar + NavHost）。仅需首页占位页面存在且能添加导航栏按钮即可。 |

### R-05：快捷入口「演员」和「新剧」跳转目标已定义

| 属性 | 值 |
|------|-----|
| **维度** | 可落地性 |
| **严重程度** | 🔴 阻塞 |
| **状态** | ✅ 已修正 | **修正内容：** 
- R-01: PRD 中所有「5 个」改为「4 个」，删除省略号，对齐实际快捷入口数量
- R-02: 新增 4.2（US-03 热搜榜浏览）和 4.3（US-04 快捷入口导航）核心流程章节，包含用户旅程和关键边界表
- R-03: ST-04 前置依赖补充 `PRD-01 ST-05`（Android NavHost 路由框架）
- R-04: ST-05/ST-06 前置依赖改为路由框架（PRD-01 ST-01/ST-02/ST-04/ST-05），不再依赖完整首页 Feed UI
- R-05: 「演员」和「新剧」入口已定义跳转行为：点击展示 Toast「功能开发中」
- R-06: Q-01 结论改为「首版搜标题+分类（category）」对齐现有 Schema
- R-07: ST-02 热搜 API 数据来源明确为首版硬编码种子数据，不与搜索 API 绑定
- R-08: ST-03/ST-04 搜索结果页明确复用首页 Feed 卡片组件
| **问题描述** | US-04 快捷入口包括「排行/新剧/分类/演员」4 个入口，但 PRD 第 5 节依赖表仅列出排行页（PRD-05）和分类页（PRD-06）作为跳转目标。第 1.4 节将「演员搜索独立页」列为范围外（「先作为搜索结果中的标签」），这意味着「演员」入口没有可跳转的目标页面。「新剧」入口的目标也未在任何 PRD 中定义。 |
| **建议修正** | 两个候选方案：(a) 阶段一仅保留有明确目标的入口（排行→PRD-05、分类→PRD-06），演员和新剧入口标记为灰色/禁用状态，后续迭代时激活；(b) 「新剧」入口跳转到排行页的「新剧榜」Tab（若 PRD-05 包含此 Tab），并在依赖表中注明；「演员」入口暂不展示或显示「功能开发中」Toast。推荐 (a)，并在范围定义中注明。 |

### R-06：搜索匹配字段与现有 Schema 不一致

| 属性 | 值 |
|------|-----|
| **维度** | 可落地性 |
| **严重程度** | 🟡 建议 |
| **状态** | ✅ 已修正 | **修正内容：** 
- R-01: PRD 中所有「5 个」改为「4 个」，删除省略号，对齐实际快捷入口数量
- R-02: 新增 4.2（US-03 热搜榜浏览）和 4.3（US-04 快捷入口导航）核心流程章节，包含用户旅程和关键边界表
- R-03: ST-04 前置依赖补充 `PRD-01 ST-05`（Android NavHost 路由框架）
- R-04: ST-05/ST-06 前置依赖改为路由框架（PRD-01 ST-01/ST-02/ST-04/ST-05），不再依赖完整首页 Feed UI
- R-05: 「演员」和「新剧」入口已定义跳转行为：点击展示 Toast「功能开发中」
- R-06: Q-01 结论改为「首版搜标题+分类（category）」对齐现有 Schema
- R-07: ST-02 热搜 API 数据来源明确为首版硬编码种子数据，不与搜索 API 绑定
- R-08: ST-03/ST-04 搜索结果页明确复用首页 Feed 卡片组件
| **问题描述** | PRD 第 7 节 Q-01 结论为「首版搜标题+标签」，subtasks ST-01 也写「标题+标签 LIKE 搜索」。但根据 wiki 确认，当前 Drama Schema 只有 `title`（标题）和 `category`（分类，单个字符串）字段，没有 `tags`（标签，复数）字段。 |
| **建议修正** | 将 Q-01 结论改为「首版搜标题+分类（category）」以匹配现有 Schema，或在第 5 节依赖中新增条目「Drama Schema 扩展：新增 tags 字段（数组）」，标注状态为「🚧 待开发」。 |

### R-07：ST-02 热搜 API 数据来源逻辑需明确

| 属性 | 值 |
|------|-----|
| **维度** | 可落地性 |
| **严重程度** | 🟡 建议 |
| **状态** | ✅ 已修正 | **修正内容：** 
- R-01: PRD 中所有「5 个」改为「4 个」，删除省略号，对齐实际快捷入口数量
- R-02: 新增 4.2（US-03 热搜榜浏览）和 4.3（US-04 快捷入口导航）核心流程章节，包含用户旅程和关键边界表
- R-03: ST-04 前置依赖补充 `PRD-01 ST-05`（Android NavHost 路由框架）
- R-04: ST-05/ST-06 前置依赖改为路由框架（PRD-01 ST-01/ST-02/ST-04/ST-05），不再依赖完整首页 Feed UI
- R-05: 「演员」和「新剧」入口已定义跳转行为：点击展示 Toast「功能开发中」
- R-06: Q-01 结论改为「首版搜标题+分类（category）」对齐现有 Schema
- R-07: ST-02 热搜 API 数据来源明确为首版硬编码种子数据，不与搜索 API 绑定
- R-08: ST-03/ST-04 搜索结果页明确复用首页 Feed 卡片组件
| **问题描述** | ST-02 描述热搜 API 返回「搜索次数最多的关键词」，但同时又依赖 ST-01（搜索 API）。搜索次数统计需要搜索行为数据积累，而搜索 API 刚实现时没有历史数据。虽然工作内容提到「种子数据预设热搜词」，但未说明与搜索 API 的关系：热搜是纯静态种子数据，还是从搜索记录中统计？ |
| **建议修正** | 在 ST-02 的工作内容中明确：「首版热搜词使用种子数据硬编码预设（如：逆袭、甜宠、战神、穿越…），不依赖搜索 API 统计数据。后续版本接入搜索日志统计，实现动态热搜。」同时评估是否可以解除 ST-02 → ST-01 的依赖。 |

### R-08：搜索结果卡片组件复用策略未明确

| 属性 | 值 |
|------|-----|
| **维度** | 跨端一致性 |
| **严重程度** | 🟡 建议 |
| **状态** | ✅ 已修正 | **修正内容：** 
- R-01: PRD 中所有「5 个」改为「4 个」，删除省略号，对齐实际快捷入口数量
- R-02: 新增 4.2（US-03 热搜榜浏览）和 4.3（US-04 快捷入口导航）核心流程章节，包含用户旅程和关键边界表
- R-03: ST-04 前置依赖补充 `PRD-01 ST-05`（Android NavHost 路由框架）
- R-04: ST-05/ST-06 前置依赖改为路由框架（PRD-01 ST-01/ST-02/ST-04/ST-05），不再依赖完整首页 Feed UI
- R-05: 「演员」和「新剧」入口已定义跳转行为：点击展示 Toast「功能开发中」
- R-06: Q-01 结论改为「首版搜标题+分类（category）」对齐现有 Schema
- R-07: ST-02 热搜 API 数据来源明确为首版硬编码种子数据，不与搜索 API 绑定
- R-08: ST-03/ST-04 搜索结果页明确复用首页 Feed 卡片组件
| **问题描述** | PRD 第 4.1 节 UI 表格描述搜索结果页为「结果列表（卡片同 Feed 卡片）」，暗示搜索结果卡片应与首页 Feed 卡片组件一致。但 subtasks ST-03/ST-04 的工作内容中仅写「结果列表」，未明确是复用 `FeedCardView`/`FeedCard` 还是独立实现。 |
| **建议修正** | 在 ST-03 和 ST-04 的工作内容中补充一条：「搜索结果列表复用首页 Feed 卡片组件（`FeedCardView` / `FeedCard`），统一视觉风格。」 |

---

## 待澄清问题

| 编号 | 问题 | 阻塞 | 上下文 |
|------|------|------|--------|
| Q-01 | 搜索匹配维度：仅标题？还是也搜描述/标签/演员？ | 否 | 已在 PRD 中，结论：首版搜标题+标签（但标签字段不存在，见 R-06） |
| Q-02 | 「新剧」快捷入口跳转目标是什么？ | ✅ 已澄清：点击展示 Toast「功能开发中」，后续迭代激活。 |
| Q-03 | 搜索页在无搜索历史且热搜 API 异常（网络不可用）时，展示什么？仅展示快捷入口 + 热搜区域显示「加载失败，点击重试」？ | 否 | 关键边界表未覆盖此场景 |

---

## 变更历史

| 日期 | 变更内容 | 变更原因 |
|------|---------|---------|
| 2026-07-25 | 初始审查 | PRD-04 审查 |
