# Spec 审查报告：PRD-04 搜索发现

> 审查日期：2026-07-26
> 审查轮次：1
> 对应文档：`docs/specs/2026-07-26-prd-04-search-discovery/spec.md`

---

## 1. 审查结果总览

| 项目 | 结果 |
|------|------|
| 审查结论 | ✅ 通过，可进入 `spec-human-review` |
| 审查轮次 | 1 |
| 发现问题总数 | 6 |
| 已修复 | 6 |
| 遗留阻塞问题 | 0 |
| 非阻塞待确认项 | 1（热搜热度值展示格式） |

本轮审查聚焦以下维度：
- 需求范围与当前代码事实是否一致
- 路由 / deeplink / 页面承接是否可落地
- Backend API 契约是否足够精确
- 搜索历史、热搜、结果页交互是否前后一致
- 是否具备最小自动化验收标准

结论：首轮审查识别的 6 个问题均已在 `spec.md` 中完成收敛，当前文档已具备进入人工确认阶段的条件。

---

## 2. 发现的问题

| 编号 | 严重程度 | 问题描述 | 主 agent 可自行修复 | 修复状态 |
|------|----------|----------|----------------------|----------|
| SR-01 | 高 | 搜索发现页、搜索结果页与快捷入口缺少可落地的 Native route / deeplink / 承接契约，存在误落到 Web 占位页的风险 | 是 | ✅ 已修复 |
| SR-02 | 高 | 搜索历史写入时机前后矛盾：手动搜索、历史词搜索、热搜词搜索对“何时写历史”描述不一致 | 是 | ✅ 已修复 |
| SR-03 | 高 | “新剧 / 演员”首版承接方案包含当前数据能力不支持的选项，尤其演员搜索不具备现成 schema / API 支撑 | 否（需产品决策） | ✅ 已修复 |
| SR-04 | 高 | `GET /api/dramas/search` 与 `GET /api/dramas/hot-search` 的请求参数、响应结构、错误码与分页边界定义不够精确 | 是 | ✅ 已修复 |
| SR-05 | 中 | 搜索结果页交互语义与现有首页可复用卡片事实不一致，spec 混用了“整卡点击”与“观看 / 详情”双动作 | 是 | ✅ 已修复 |
| SR-06 | 中 | 缺少 Backend / iOS / Android 的最小自动化验收要求，后续 coding / QA 难以对齐 | 是 | ✅ 已修复 |

---

## 3. 上一轮问题修复验证

| 编号 | 修复状态 | 验证结果 | 关联修复 |
|------|----------|----------|----------|
| SR-01 | ✅ 已修复 | `spec.md` 已明确搜索发现页、搜索结果页、排行 / 分类 / 新剧 / 演员的 Android route、iOS route 语义与 deeplink 约束，并明确全部为 Native 页面承接，不复用 Web `/search` 或 `/rankings` 占位页 | 5.4 流程描述、术语表、兼容性要求 |
| SR-02 | ✅ 已修复 | `spec.md` 已统一为：**仅当搜索请求成功返回时（包含空结果，不包含失败）才写入本地历史**；手动搜索、热搜点击与历史规则描述已对齐 | 5.1、5.2、5.3 流程与 mermaid 图 |
| SR-03 | ✅ 已修复 | 已根据已收集的人类决策，将“新剧 / 演员”首版收口为 **Native 占位承接页**；不再假设演员真实搜索或新剧独立后端能力 | 4. 用户故事、5.4 流程描述、10. 待澄清问题 |
| SR-04 | ✅ 已修复 | `spec.md` 已补齐搜索 / 热搜 API 的参数、返回结构、错误码与边界行为，其中搜索接口复用现有 `DramaListResponse`，热搜接口返回 `{ data: HotSearchItem[] }` | 5.1、6. 数据概览、9. 依赖 |
| SR-05 | ✅ 已修复 | 搜索结果页已明确复用现有首页 Drama 卡片交互语义，首版只保留“观看 / 详情”两个动作，不新增整卡点击语义 | 4. 用户故事、5.1、5.5 |
| SR-06 | ✅ 已修复 | `spec.md` 已新增 Backend / iOS / Android 的最小自动化验收要求，覆盖 route handler / ViewModel / route / deeplink / 状态机等核心点 | 8.4 最小自动化验收要求 |

---

## 4. 遗留问题（需人工决策）

当前无阻塞进入 `spec-human-review` 的遗留问题。

仍保留 1 个**非阻塞**待确认项：

| 编号 | 问题 | 默认值 | 是否阻塞 |
|------|------|--------|----------|
| NQ-01 | 热搜热度值首版是否需要展示单位（如“万热度”） | 默认仅展示数值 | 否 |

---

## 5. 修改记录

| 时间 | 操作 | 说明 |
|------|------|------|
| 2026-07-26 | 首轮 spec-review | 识别 6 个问题，涵盖 route/deeplink、历史写入时机、快捷入口承接、API 契约、结果页交互与自动化验收 |
| 2026-07-26 | 主 agent 修复 spec | 已将 6 个问题全部回写到 `spec.md`，并同步更新默认值与非阻塞待确认项 |
| 2026-07-26 | 修复验证 | 确认当前版本已满足进入 `spec-human-review` 的条件 |

---

## 6. 结论

- 本轮 `spec-review` 已完成收敛。
- 当前 `spec.md` 与现有代码事实、产品边界及后续 PRD 承接关系已基本对齐。
- 可以推进到 `spec-human-review`。
- 后续进入 design 阶段时，应继续遵守以下约束：
  - `mall` / `earn` 仍为 H5 边界；
  - 其他业务页当前按 Native 页面实现；
  - 不新增未经批准的开源依赖；
  - 不改变既有 `play` canonical route 与 Android `player` alias 兼容策略。
