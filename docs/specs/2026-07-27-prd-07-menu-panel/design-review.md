# 技术方案 Review：PRD-07 菜单面板

> Review 日期：2026-07-27
> Review 循环：第 2 轮
> 审查者：AI Agent

## 审查结果总览

### Shared 设计 (design.md)

| 维度 | 检查项数 | 通过 | 问题 | 已修复 |
|------|---------|------|------|--------|
| 与 Spec 一致性 | 6 | 6 | 0 | 0 |
| 功能完整性 | 5 | 5 | 0 | 0 |
| API 完整性 | 4 | 4 | 0 | 0 |
| 数据模型一致性 | 3 | 3 | 0 | 0 |
| 边界与错误处理 | 5 | 5 | 0 | 0 |
| 安全考虑 | 4 | 4 | 0 | 0 |
| 性能考虑 | 3 | 3 | 0 | 0 |

### 平台设计 (design-{platform}.md)

| 平台 | 与 Spec 一致性 | 功能完整性 | 架构 | 文件变更 | API 调用 | 状态管理 | 测试策略 | 总体 |
|------|--------------|----------|------|---------|---------|---------|---------|------|
| Backend | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| iOS | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Android | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Web | N/A（范围外） | N/A（范围外） | N/A（范围外） | N/A（范围外） | N/A（范围外） | N/A（范围外） | N/A（范围外） | N/A（范围外） |

## 发现的问题

### Shared 设计问题

本轮未发现 Shared 设计新增阻塞/关注问题。

### 平台设计问题

本轮按人工决策收敛后，backend 最近在看允许在脏数据过滤后少于 3 条，已与 `spec.md`、`design.md`、`design-backend.md` 保持一致；不再保留阻塞/关注问题。

## 跨端一致性检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| API 调用与 Shared 设计一致 | ✅ | iOS / Android 的 recently-viewed 请求头与接口路径都与 shared design 对齐 |
| 数据模型各端一致 | ✅ | recently-viewed 字段集在 shared、backend、iOS、Android 间保持一致 |
| 共享逻辑覆盖 | ✅ | iOS / Android 都已把“关闭抽屉动画完成后再导航”落实为可执行状态机与测试策略 |
| 错误处理策略一致 | ✅ | 各端都维持最近在看区局部 loading / empty / error，不影响静态区块 |

## 上一轮问题修复验证

> 本轮为非首轮 review。已对上一轮报告中标记为 `🔄 已修复，待验证` 的问题进行验证。

| 问题编号 | 原问题摘要 | 所属方案 | 原修复状态 | 验证结果 | 说明 |
|---------|-----------|---------|-----------|---------|------|
| backend-1 | 最近在看聚合先 limit 再过滤脏数据，会少返回有效条目 | design.md / design-backend.md | 🔄 已修复，待验证 | ✅ 已按决策收敛 | 已统一为“固定候选窗口内过滤脏数据后最多返回 3 条，允许不足 3 条”的契约，spec / shared / backend 口径一致 |
| ios-1 | 关闭抽屉后再导航的时序没有落实到可执行机制 | design-ios.md | 🔄 已修复，待验证 | ✅ 已验证修复 | 已补充 `closeMenuPanelThenNavigate(to:) + markMenuPanelDidClose()` 机制、closing 防重入规则及对应测试场景 |
| android-1 | 抽屉关闭与导航的串行动作缺少动画完成约束 | design-android.md | 🔄 已修复，待验证 | ✅ 已验证修复 | 已补充 `closeMenuThenNavigate(targetRoute) + onMenuClosedAnimationFinished()` 串行机制及 closing 防重入测试 |

若某个问题验证结果为 `⚠️ 修复不完整` 或 `❌ 未修复`，应同时在「发现的问题」中新增对应条目，维度标注为「验证-未修复」或「验证-修复不完整」。

## 遗留问题（需人工决策）

本轮无需要人工决策的遗留问题。

## 结论

- [x] ✅ 所有问题已修复，可进入下一阶段（design-human-review）
- [ ] ⚠️ 仍存在问题，建议修复后重新发起 design-review

补充说明：
- 上一轮 3 个问题均已收敛，其中 backend 问题按人工决策改为“允许脏数据过滤后不足 3 条”，并已同步到 spec / shared / backend。
- 当前无剩余关注问题，可进入 `design-human-review`。
