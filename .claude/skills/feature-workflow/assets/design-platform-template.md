# {{平台}}技术方案：{{功能名称}}

> 创建日期：{{YYYY-MM-DD}}
> 对应共享方案：design.md
> 对应需求：spec.md

## 架构设计

<!-- 本端架构图，组件/模块关系 -->

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   UI Layer   │───▶│  ViewModel   │───▶│   Service    │
│  Components  │◀───│  / Presenter │◀───│  / Repo      │
└──────────────┘    └──────────────┘    └──────────────┘
```

## 核心文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `{{platform}}/.../xxx.ts` | 新增 | |
| `{{platform}}/.../xxx.ts` | 修改 | |

## 组件/模块设计

### {{组件1名称}}

- **职责**：
- **Props/参数**：
- **状态**：
- **对外接口**：

## 路由/导航

| 路由 | 页面/组件 | 说明 |
|------|----------|------|
| | | |

## 状态管理

| 状态 | 存储方式 | 作用域 | 说明 |
|------|---------|--------|------|
| | | | |

## API 调用

| 接口 | 调用时机 | 错误处理 |
|------|---------|---------|
| `METHOD /api/path` | | |

## 边界与错误处理（⚠️ 重点，最易遗漏）

### 错误处理策略

- **全局错误拦截器/中间件**：
- **错误码映射表**（后端错误码 → 端侧用户提示文案）：

| 后端错误码 | 用户提示文案 | 处理方式 |
|-----------|------------|---------|
| `UNAUTHORIZED` | | Toast / 跳转登录 |
| `NETWORK_ERROR` | | Toast + 重试按钮 |
| `INTERNAL_ERROR` | | Toast / 错误页面 |

### 端侧特有边界场景

| 场景 | 触发条件 | 处理策略 |
|------|---------|---------|
| 网络切换（Wi-Fi ↔ 蜂窝） | | |
| App 进入后台/返回前台 | | |
| 页面销毁时未完成请求 | | 取消请求 / 忽略回调 |
| 本地缓存过期/损坏 | | |
| 用户快速连续操作 | | 防抖 / 节流 |

### UI 态覆盖

| 状态 | UI 表现 | 触发条件 |
|------|--------|---------|
| 加载中 | | |
| 加载成功（有数据） | | |
| 加载成功（空数据） | | |
| 加载失败（可重试） | | |
| 加载失败（不可重试） | | |

## 测试策略

| 测试类型 | 覆盖范围 | 工具 |
|---------|---------|------|
| 单元测试 | | |
| 集成测试 | | |

## 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| | | |
