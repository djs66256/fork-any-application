# 采集文档模板

> 本文件在**规划阶段**由主 agent 预制，填入采集信息和操作序列。用户审批通过后，
> 采集 subagent 直接读取本文件，按操作序列执行，并将观察、日志、产物清单、异常等回填到对应位置。

---

# 采集：{{竞品名称}} — {{采集描述}}

## 采集信息

| 项目 | 内容 |
|------|------|
| 采集日期 | {{YYYY-MM-DD}} |
| 目标竞品 | {{竞品名称}} |
| 竞品版本 | {{版本号，不可获取时注明}} |
| 包名/标识 | {{竞品包名}} |
| 频道 | {{mobile / web}} |
| 采集方案 | {{ADB / Playwright / ...}} |
| 功能模块 | {{功能模块}} |
| 采集范围 | {{本次采集覆盖的操作和场景}} |

## 命令参考

采集过程中的命令参考 `references/{{频道}}-{{方案}}.md`（如 `mobile-adb.md`）。采集 subagent 已加载 product-research skill，可通过 skill 目录定位该文件。

## 操作序列

> 以下步骤在规划阶段填入。采集 subagent 逐步骤执行，每步完成后填写「观察」栏和截图路径。

### 步骤 1：{{操作描述}}

- **操作**：{{用户操作描述}}
- **命令**：`{{命令}}`
- **截图**：保存为 `assets/{{日期}}-step-01-{{描述}}.png`
- **观察要点**：{{需要关注的行为细节}}

*执行后由 subagent 填写：*
- **观察**：{{当前页面状态、实际行为、与预期的差异}}
- **截图文件**：`{{实际保存的文件名}}`

### 步骤 2：{{操作描述}}

- **操作**：{{用户操作描述}}
- **命令**：`{{命令}}`
- **截图**：保存为 `assets/{{日期}}-step-02-{{描述}}.png`
- **观察要点**：{{需要关注的行为细节}}

*执行后由 subagent 填写：*
- **观察**：
- **截图文件**：

<!-- 按需增加更多步骤 -->

## 录屏

> 规划阶段标注是否需要录屏及覆盖范围。

{{如不需要录屏则写"本次采集无需录屏"；如需要则写：}}

- **录屏范围**：{{从步骤 N 到步骤 M}}
- **保存为**：`assets/{{日期}}-{{描述}}.mp4`

*执行后由 subagent 填写：*
- **录屏文件**：{{实际保存的文件名}}

## 产物清单

> 采集 subagent 执行完成后回填此表。

| 文件 | 类型 | 对应步骤 | 说明 |
|------|------|---------|------|
| `assets/{{文件名}}` | {{截图/录屏}} | 步骤 N | {{说明}} |

## 采集日志

> 采集 subagent 执行过程中逐条记录。

| 时间 | 操作 | 命令 | 结果 |
|------|------|------|------|
| HH:MM:SS | 打开应用 | `{{命令}}` | {{成功/失败}} |
| HH:MM:SS | {{操作}} | `{{命令}}` | {{结果}} |

## 异常记录

> 采集 subagent 回填。如有操作失败、页面异常、定位偏差等在此记录。

{{如无异常则标注"无异常"}}

---

> 本文件在规划阶段预制采集信息和操作步骤，由采集 subagent 执行后回填观察、日志、产物清单和异常。
> 不作为分析推断，分析内容见同目录 `analysis.md`。
